// pdf_export.dart — export warped document + extracted fields to PDF.
// TODO(week 4): use package:pdf + package:printing for save/share.
import 'package:image/image.dart' as img;
class PdfExport {
  static Future<String> exportDocument(img.Image doc, Map<String,String> fields) async {
    throw UnimplementedError('Implement in week 4.');
  }
}
