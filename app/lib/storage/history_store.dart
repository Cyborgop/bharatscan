// history_store.dart — sqflite-backed scan history.
// TODO(week 4): CRUD for scans table, optional AES-256 encryption via
// sqflite_sqlcipher. Schema: id, created_at, doc_type, thumbnail_path,
// pdf_path, fields_json.
class HistoryStore {
  Future<void> init() async {}
}
